# Figure reproduction recipes

Auto-generated from [`docs/figure_manifest.yaml`](docs/figure_manifest.yaml) by `tools/gen_docs.py` — **do not edit by hand.**

How each paper figure is produced. Notebooks live in `notebooks/`, importable helpers in the `ring/` package, cached inputs in `data/`, and figure outputs in `figures/` (see each figure's *Output*). Some composite-figure panels are intermediate files gated behind `SAVE_FIGURES` (default off) to protect the locked baseline; they are not kept on disk and are regenerated by setting `SAVE_FIGURES = True` and running the producer cell. Reproducibility caveats are collected at the [end](#reproducibility-caveats).

## Terms used here

**Figure kind**
- *direct* — a single notebook cell saves the whole figure; reproducible from the notebook alone.
- *composite* — several panels are drawn by one notebook cell and assembled into the final image in code.
- *response* — an additional figure produced by the notebooks; not a numbered figure in the paper.

**Verification verdict** — how faithfully re-running the code reproduces the committed figure:
- *BIT-EXACT* — regenerates a byte-identical file.
- *PDF-METADATA-ONLY* — identical image; only PDF timestamps and ids differ.
- *NON-DETERMINISTIC* — small run-to-run variation remains even with a fixed seed; the per-figure note says why.

**Other terms**
- *Producer* — the notebook, and specific cell, that generates a figure or panel.
- *Input caches* — precomputed results in `data/` that a notebook loads instead of recomputing.

## Figure 1 — Classical ring-attractor model and mouse HD tuning curves

- **Manuscript file:** `figures/classical_attractor_vs_data.pdf`
- **Kind:** composite
- **Producer notebook:** `notebooks/data_and_generative_model.ipynb`
- **Output:** `classical_attractor_vs_data.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/classical_attractor_vs_data.pdf", dpi=400)`
- **Input caches:** `data/mouse_data/tc_data.npz`
- **Reproduce:** run `notebooks/data_and_generative_model.ipynb` top-to-bottom; the figure is written to `figures/classical_attractor_vs_data.pdf`.
- **Composite panels:**
    - a (ring-attractor schematic): ring.plot.plot_ring_schematic
    - b (circulant weight matrix, Mexican-hat J0 + J1 cos profile): ring.plot.plot_circulant_weights
    - c (translation-invariant tuning curves): ring.plot.plot_circulant_tuning_curves
    - d (flow of the paper): ring.plot.plot_flow_schematic
    - e (per-mouse data tuning curves): ring.plot.plot_data_tuning_curves
    - f/g (mean + std profiles): ring.plot.plot_mean_std_profiles
    - composed and written by one notebook cell -> figures/classical_attractor_vs_data.pdf
- **Verification:** BIT-EXACT
- **Notes:** Panel b shows the classical ring-attractor connectivity kernel J(theta - theta') = J0 + J1 cos(theta - theta') with J0 = 0, J1 = 1; the color limits are symmetric about zero so white marks the sign change, and there is no colorbar because the schematic carries no weight units. The whole figure, including the ring schematic (drawn by ring.plot.plot_ring_schematic) and panels a-g, is produced by a single notebook cell and reproduces as a bit-identical PNG when the cell is rerun. The top-band panels (a-d) are placed with explicit fig.add_axes calls rather than GridSpec, since GridSpec would give panels a and b different heights when b is square; panel a's y-window is pinned to the ring's outer diameter so the ring renders at the heatmap's size. Rendering the somata in the ring schematic requires interpolation='bilinear', overriding the global interpolation='none' default set in ring/style.py. The unit sampling in plot_data_tuning_curves is seeded by row index, so panel c is deterministic given the input order. Input cache tc_data.npz regenerates bit-exactly, and panel letters are lowercase (a-g), matching the captions.

## Figure 2 — Data-driven ring attractor: PCA convergence, connectivity, closed-loop integration

- **Manuscript file:** `figures/data_driven_attractor.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_driven_attractor.ipynb`
- **Output:** `data_driven_attractor.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/data_driven_attractor.pdf", dpi=400)`
- **Input caches:** `data/closing_the_loop.npz`, `data/pcs_updated_again.npz`, `data/distances_updated_again.npz`, `data/simulation_results_updated_again.npz`, `data/spectra_results_updated_again.npz`, `data/integration_sim_results_updated_again.npz`
- **Reproduce:** run `notebooks/data_driven_attractor.ipynb` top-to-bottom; the figure is written to `figures/data_driven_attractor.pdf`.
- **Verification:** BIT-EXACT
- **Notes:** Produced by a single notebook cell that consumes closing_the_loop.npz (from closing_the_loop.ipynb) and five _updated_again caches. The scrambled-noise shuffle and the convergence-analysis initial-condition noise are each seeded, so the circulant-plus- noise, PCA-convergence, and activity-bump subpanels, along with the closing_the_loop- derived portion of the figure, all reproduce bit-exactly from the committed caches. Connectivity and covariance matrices use RdBu_r; the m(theta,t) scalar activity fields use viridis. Panel letters are lowercase (a-l), matching the captions and callouts.

## Figure 3 — Distributional circular symmetry in head-direction responses

- **Manuscript file:** `figures/circular_symmetry_in_data.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_and_generative_model.ipynb`
- **Output:** `circular_symmetry_in_data.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/circular_symmetry_in_data.pdf", dpi=400)`
- **Input caches:** `data/mouse_data/tc_data.npz`, `data/grid_search_results.npz`
- **Reproduce:** run `notebooks/data_and_generative_model.ipynb` top-to-bottom; the figure is written to `figures/circular_symmetry_in_data.pdf`.
- **Verification:** BIT-EXACT
- **Notes:** All three panels are produced by a single notebook cell in data_and_generative_model.ipynb, gated by SAVE_FIGURES. Panel b's matched-uniform null is drawn after calling ring.seed.set_global_seed(42), the only place in this figure that draws random numbers, so the committed panel is the first draw from that seed and reproduces bit-exactly; panels a and c are fully deterministic. Panel c's covariance heatmaps use viridis. Panel letters are lowercase (a-c), matching the captions and callouts.

## Figure 4 — Generative-model tuning curves and connectivity (extended)

- **Manuscript file:** `figures/generative_model_tuning.pdf`
- **Kind:** composite
- **Producer notebook:** `notebooks/data_and_generative_model.ipynb`
- **Output:** `generative_model_tuning.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/generative_model_tuning.pdf")  # SAVE_FIGURES-gated`
- **Input caches:** `data/mouse_data/tc_data.npz`, `data/grid_search_results.npz`
- **Reproduce:** run `notebooks/data_and_generative_model.ipynb` top-to-bottom; the figure is written to `figures/generative_model_tuning.pdf`.
- **Composite panels:**
    - a-f (Gamma_x, GP samples, softplus, error-vs-sigma, error landscape, data/model correlation): ring.plot plot_cov_examples / plot_pre_examples / plot_nonlin_examples / plot_loss_vs_scale / plot_loss_landscape / plot_covariance_comparison
    - g (random samples from the fitted process, one independent sample per sub-panel): ring.plot.plot_model_tuning_curves
    - composed by one SAVE_FIGURES-gated code cell -> figures/generative_model_tuning.pdf
- **Verification:** PDF-METADATA-ONLY
- **Notes:** Panel g's sample grid is produced by ring.plot.plot_model_tuning_curves, which draws its samples without replacement after ring.seed.set_global_seed (default 42), giving 24 distinct tuning curves in the grid. All seven panels are assembled by one code cell using gridspec and saved directly to figures/generative_model_tuning.pdf, gated by SAVE_FIGURES. Panel e's axis labels read '$b$ [soft threshold]' and '$\beta$ [sharpness]', matching the notation used in the paper. The verdict is PDF-METADATA-ONLY: independent runs give a raster-pixel-identical PDF, with only the PDF timestamp and id differing.

## Figure 5 — Model/data comparison, mean-std profiles, and fitting

- **Manuscript file:** `figures/model_vs_data.pdf`
- **Kind:** composite
- **Producer notebook:** `notebooks/data_and_generative_model.ipynb`
- **Output:** `model_vs_data.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/model_vs_data.pdf")  # SAVE_FIGURES-gated`
- **Input caches:** `data/analysis_results.npz`, `data/mouse_data/tc_data.npz`, `data/grid_search_results.npz`
- **Reproduce:** run `notebooks/data_and_generative_model.ipynb` top-to-bottom; the figure is written to `figures/model_vs_data.pdf`.
- **Composite panels:**
    - a/b (mean + fluctuation profiles, model vs data): ring.plot.plot_mean_std_profiles
    - c (peak counts, 3 z-thresholds): ring.plot.plot_peak_count_comparison
    - d (peak-height histograms, 3 peaks): ring.plot.plot_peak_value_histograms
    - e (flip-symmetry, full + zoomed tail): ring.plot.plot_symmetry_histogram
    - f (HD information): ring.plot.plot_information_histogram
    - composed single-column by one SAVE_FIGURES-gated code cell -> figures/model_vs_data.pdf
- **Verification:** PDF-METADATA-ONLY
- **Notes:** This composite is produced by one code cell that assembles the model/data statistics, mean/std profile, and generative-model-fitting panels via add_gridspec and saves figures/model_vs_data.pdf directly, gated by SAVE_FIGURES. The generative-process panels draw null-model samples from an RNG seeded in the cell (np.random.seed(42)), so results are deterministic given that seed. In analysis_results.npz, the peak_counts arrays are bit-exact for the four experimental-data keys; the four generative-model keys are a fresh statistical draw from the same underlying distribution each time the cache is regenerated. Peak detection checks the final angular bin, so every curve contributes at least one peak, as a circular signal must. The verdict is PDF-METADATA-ONLY: regenerating from the notebook reproduces the committed PDF pixel-for-pixel at 400 dpi.

## Figure 6 — Circular geometry, disordered embedding, and spectra of the optimal weights

- **Manuscript file:** `figures/disordered_vs_circulant_spectra.pdf`
- **Kind:** composite
- **Producer notebook:** `notebooks/spectra.ipynb`
- **Output:** `disordered_vs_circulant_spectra.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/disordered_vs_circulant_spectra.pdf", dpi=400)`
- **Reproduce:** run `notebooks/spectra.ipynb` top-to-bottom; the figure is written to `figures/disordered_vs_circulant_spectra.pdf`.
- **Composite panels:**
    - ring.plot.plot_embedding_schematic -> panel A (ring-embedding schematic)
    - spectra.ipynb code-cell 10 -> panels B-H (matrices, spectrum, eigenvalue planes, eigenvector scatters)
- **Verification:** BIT-EXACT
- **Notes:** J2 (the circulant-plus-noise matrix) is built by reseeding the RNG immediately before adding noise (np.random.seed(45)), and the whole figure is saved by the following code cell. The figure is fully synthetic: no cached array is read, and the tuning curves come from gen_tuning_curves_idealized. Reproducibility caveat: panel g's eigenvector/SVD panel can show a sign-flip in its rightmost subplot when regenerated on different hardware or BLAS builds, because singular vectors are only defined up to sign and the seed does not fix that. The spectra shown use ridge regularization lambda = 1e-8, computed as Gamma_x/(Gamma_phi + lambda); leading eigenvalues and the doublet/singlet structure are stable under this choice.

## Figure 7 — DMFT theory of the disordered ring attractor

- **Manuscript file:** `figures/dmft_theory.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/dmft.ipynb`
- **Output:** `dmft_theory.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/dmft_theory.pdf", dpi=400)`
- **Input caches:** `data/jacobian_results.npz`, `data/dmft_simulation_results.npz`
- **Reproduce:** run `notebooks/dmft.ipynb` top-to-bottom; the figure is written to `figures/dmft_theory.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** The figure is produced directly by one code cell that reads jacobian_results.npz and dmft_simulation_results.npz, both bit-exact deterministic caches, with no RNG involved anywhere in the pipeline. Panels b, c and f use (sigma, beta) = (1.4211, 2.7659), matching the Methods. DMFTModel's regularization constant lamda defaults to 1e-12, a numerical guard against the continuum inversion becoming singular as Ghat^phi_n -> 0, distinct from the larger finite-N fitting ridge used elsewhere in the codebase. The verdict is PDF- METADATA-ONLY: regenerating gives a bit-identical raster once PDF timestamps and ids are stripped.

## Figure 8 — Grid-cell simulations from the disordered attractor

- **Manuscript file:** `figures/grid_cell_simulations.pdf`
- **Kind:** composite
- **Producer notebook:** `notebooks/grid_sims.ipynb`
- **Output:** `grid_cell_simulations.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/grid_cell_simulations.pdf", bbox_inches="tight", dpi=450)  # SAVE_FIGURES-gated`
- **Input caches:** `data/grid_analysis_results.npz`, `data/eta_analysis_4.npz`, `figures/external/hafting2005_grid_ratemaps.png`, `data/gridness_hist_digitized.npz`
- **Reproduce:** run `notebooks/grid_sims.ipynb` top-to-bottom; the figure is written to `figures/grid_cell_simulations.pdf`.
- **Composite panels:**
    - a (experimental grid-cell responses, Hafting et al. 2005): published bitmap loaded from figures/external/hafting2005_grid_ratemaps.png, used with permission from Springer Nature
    - b (experimental grid-score distribution, Nayebi 2021 / Mallory 2021): histogram of data/experimental_gridness_scores.npy, digitized from the published figure
    - c-f (grid-score distributions, rate maps, autocorrelograms, torus PCA): grid_sims simulation content, rendered in the compose cell
    - assembled by one SAVE_FIGURES-gated code cell -> figures/grid_cell_simulations.pdf
- **Verification:** BIT-EXACT
- **Notes:** Panels a-b are external images, not model output: panel a is the Hafting et al. 2005 rat grid-cell rate maps, used with permission from Springer Nature, loaded from figures/external/hafting2005_grid_ratemaps.png, and panel b is a bar plot of the digitized Nayebi et al. 2021 / Mallory et al. 2021 grid-score histogram (data/gridness_hist_digitized.npz). Panels c-f are assembled in code from the simulation cache data/grid_analysis_results.npz, distinct from the similarly named data/analysis_results.npz used by another figure. The panel-d/e example cells are drawn using a numpy RNG seeded once at the top of the notebook, so the figure is deterministic given that seed; because each example cell is chosen as the one nearest a target grid score, a different seed or ridge value changes which cells are shown rather than the qualitative result. The displayed panel-d input-current maps are flipped in sign to positive skewness where needed, since the underlying generative process is sign-symmetric and only the positively skewed realization looks like a grid cell once rectified; this flip leaves the grid scores in panel c and the autocorrelations in panel e exactly unchanged. Panel f does not plot raw PCA axes: the leading PCA components are defined only up to sign and are mutually near-degenerate in eigenvalue, so which three define a standard PCA projection is arbitrary, and the panel instead uses a fixed torus display frame pinned to the two known toroidal phases, with axes labeled torus 1/2/3 rather than PC 1/2/3.

## Figure S1 — Low-rank connectivity: kappa trajectories

- **Manuscript file:** `figures/low_rank_connectivity.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `low_rank_connectivity.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/low_rank_connectivity.pdf"`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/low_rank_connectivity.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** Illustrative synthetic figure; no cached data is read. The low-rank connectivity matrix is built with a seeded numpy RNG (np.random.seed(42)) one cell before the plotting cell, so the panel is deterministic given that seed. Regenerating the PDF reproduces the identical figure; any byte differences are confined to PDF metadata, not the rendered content.

## Figure S2 — Mixture loadings: the attractor is the full torus

- **Manuscript file:** `figures/low_rank_mixture_torus.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `low_rank_mixture_torus.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/low_rank_mixture_torus.pdf"`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/low_rank_mixture_torus.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** Illustrative synthetic figure with no cached data. The connectivity uses the same rank- four geometry and the same seeded RNG (np.random.seed(42)) as Fig. S1, but the neuron loadings are drawn from a two-component Gaussian mixture so each neuron loads onto only one doublet; this makes every ring stable simultaneously and the attractor the full torus, rather than Fig. S1's union of rings. Initial conditions use independent uniform random angles in both doublets. Layout, kappa normalization, colormap, and axis limits are copied from Fig. S1 so the two figures read as a pair. The figure is deterministic given the seed, and the save is gated on SAVE_FIGURES.

## Figure S3 — Left and right singular vectors of the connectivity

- **Manuscript file:** `figures/left_right_singular_vectors.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `left_right_singular_vectors.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/left_right_singular_vectors.png"`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/left_right_singular_vectors.png`.
- **Verification:** BIT-EXACT
- **Notes:** The plotting cell uses U and V from the singular value decomposition computed in the prerequisite cell, where V is obtained as V = Vt.T; that cell also seeds the numpy RNG once before generating the tuning curves, so the singular-vector scatter is deterministic given the seed. Regenerating the notebook reproduces the committed PNG bit-for-bit.

## Figure S4 — Real vs surrogate connectivity matrices and their spectra

- **Manuscript file:** `figures/real_vs_surrogate_matrices.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `real_vs_surrogate_matrices.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/real_vs_surrogate_matrices.png"`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/real_vs_surrogate_matrices.png`.
- **Verification:** BIT-EXACT
- **Notes:** Illustrative synthetic figure; no cached data is read. The prerequisite cell seeds the global numpy RNG (np.random.seed(42)) before generating the tuning curves used to build the actual connectivity matrix, and separately seeds a local RNG (np.random.default_rng(42)) for the orthogonal-matrix draw used to build the surrogate matrices; both draws make the figure deterministic given the seeds. The surrogate-matrix panels are identical every run; only the actual-J heatmap and its singular-value curve depend on the seeded tuning-curve draw.

## Figure S5 — Origin of the Mexican-hat connectivity profile

- **Manuscript file:** `figures/mexican_hat_origin.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/mexican_hat_and_asymmetry.ipynb`
- **Output:** `mexican_hat_origin.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/mexican_hat_origin.pdf"`
- **Reproduce:** run `notebooks/mexican_hat_and_asymmetry.ipynb` top-to-bottom; the figure is written to `figures/mexican_hat_origin.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** The plotting cell (self-contained, no direct random calls) depends on tuning curves generated upstream from a numpy RNG seeded once at the top of the notebook, so the curve is deterministic given that seed. A separate, hardcoded 1e-6 regularization constant guards a deconvolution of normalized probability densities in this cell; it is a different regularizer from the ridge used elsewhere in the codebase and unrelated to it. Regenerating reproduces the committed PDF pixel-identically, with only PDF metadata differing.

## Figure S6 — Asymmetric connectivity models

- **Manuscript file:** `figures/asymmetric_models.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/mexican_hat_and_asymmetry.ipynb`
- **Output:** `asymmetric_models.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/asymmetric_models.png"`
- **Reproduce:** run `notebooks/mexican_hat_and_asymmetry.ipynb` top-to-bottom; the figure is written to `figures/asymmetric_models.png`.
- **Verification:** BIT-EXACT
- **Notes:** The plotting cell seeds the numpy RNG (np.random.seed(42)) at its top, so the figure is deterministic given the seed. This cell uses a ridge parameter of 1e-6, not the 1e-8 used elsewhere in the codebase: the connectivity here comes from a rank-2 covariance (points on a 2D ring), so the ridge acts on only two directions and enters twice through the inverse; at 1e-8 several near-null modes are admitted and, because the heatmaps autoscale, their large magnitudes wash out the real structure. The 1e-6 value keeps the effective regularization at the level the published panels were made with, and reproduces them bit- for-bit.

## Figure S7 — Double normalization of tuning curves

- **Manuscript file:** `figures/double_normalization.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_driven_reconstruction.ipynb`
- **Output:** `double_normalization.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/double_normalization.pdf"`
- **Reproduce:** run `notebooks/data_driven_reconstruction.ipynb` top-to-bottom; the figure is written to `figures/double_normalization.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** The plotting cell seeds the numpy RNG (np.random.seed(42)) before its random neuron subsample, so the panel is deterministic given the seed; no cached data is read. The orange curve is Phi[::100] from prepare_smoothed_normalized_resampled_tuning_curves_OLD, so it reflects both the sigma = 2 Gaussian smoothing of the input currents and the cross- neuron normalization used for network construction, not the normalization alone. Regenerating reproduces the committed PDF pixel-identically, with only PDF metadata differing.

## Figure S8 — PCA convergence of the data-driven ring attractor

- **Manuscript file:** `figures/pca_convergence.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_driven_reconstruction.ipynb`
- **Output:** `pca_convergence.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/pca_convergence.png"`
- **Input caches:** `data/pc_convergence_grid.npz`
- **Reproduce:** run `notebooks/data_driven_reconstruction.ipynb` top-to-bottom; the figure is written to `figures/pca_convergence.png`.
- **Verification:** BIT-EXACT
- **Notes:** Produced by the PCA-convergence plotting cell (plot_pc_convergence_grid), gated by SAVE_FIGURES, which reads pc_convergence_grid.npz. Most of the cached arrays are exact recomputations, but the pcs_traj3_grid array is a stochastic realization, so trajectories drawn from it need not match bit-for-bit on a freshly regenerated cache even though the rest of the figure will. The sweep is over the ridge-regularization strength lambda, with the operating value lambda = 1e-8. Regenerating under a different matplotlib/Python patch version can reassign the categorical color cycle, shifting trajectory hue and draw order with no change to trajectory geometry, so small color-cycle-driven pixel differences across environments are expected.

## Figure S9 — Reconstruction error vs regularization lambda

- **Manuscript file:** `figures/error_vs_regularization.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_driven_reconstruction.ipynb`
- **Output:** `error_vs_regularization.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/error_vs_regularization.pdf")`
- **Input caches:** `data/pc_convergence_grid.npz`
- **Reproduce:** run `notebooks/data_driven_reconstruction.ipynb` top-to-bottom; the figure is written to `figures/error_vs_regularization.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** Produced by a deterministic log-log plot of err_grid[:, 0] (mean squared flow error) against the ridge-regularization values lamda_vals, both read from pc_convergence_grid.npz; there is no random component, so regenerating reproduces the rendered content exactly and any byte differences are confined to PDF metadata. The sweep spans lambda = 1e-12 to 1e-2 in decades, with the operating value lambda = 1e-8 sitting one grid step above the curve's true minimum near lambda ~ 3e-11; the apparent upturn in error below that point is a numerical-conditioning artifact of finite precision, not a property of the underlying flow, so lambda = 1e-8 should not be read as minimizing the curve. The figure is saved at full canvas width without a tight bounding box, so the surrounding whitespace is deliberate.

## Figure S10 — Noise-driven dynamics: circulant vs disordered

- **Manuscript file:** `figures/noise_driven_dynamics.png`
- **Kind:** composite
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `noise_driven_dynamics.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/noise_driven_dynamics.png", bbox_inches="tight", dpi=400)  # SAVE_FIGURES-gated`
- **Input caches:** `data/ring_attractor_noise_results.npz`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/noise_driven_dynamics.png`.
- **Composite panels:**
    - a (connectivity matrices: disordered sorted vs circulant): imshow of the cached J_sorted/J_circ; deterministic from cache
    - b (angular-drift trajectories at f_w=0.4, disordered/overlap | circ/activity): drift-trajectory logic with disordered on the left and two drift arrows drawn in code
    - c (angular stability across noise levels): make_plot grid; deterministic from cache
    - composed by one SAVE_FIGURES-gated code cell -> figures/noise_driven_dynamics.png
- **Verification:** BIT-EXACT
- **Notes:** The composite is assembled entirely in code: a single SAVE_FIGURES-gated cell in weight_matrix_analyses.ipynb draws all three panels (a: connectivity matrices, b: drift trajectories, c: angular stability) into one gridspec and saves figures/noise_driven_dynamics.png directly. All panels read deterministically from ring_attractor_noise_results.npz, produced by a REGENERATE_CACHES-gated cell seeded with numpy and torch seed 123 that is GPU-deterministic. In panel b, the disordered network is plotted on the left and the circulant network on the right, and the two drift-direction arrows are drawn as ax.annotate calls in data coordinates rather than as external artwork. Panel titles use the noise-level symbol alpha_noise, matching the caption, and panel letters are lowercase (a-c).

## Figure S11 — Spurious fixed-point search in finite-N attractors

- **Manuscript file:** `figures/spurious_fixed_points.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/spurious_fixed_points.ipynb`
- **Output:** `spurious_fixed_points.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/spurious_fixed_points.pdf"`
- **Input caches:** `data/spurious_fixed_points.npz`
- **Reproduce:** run `notebooks/spurious_fixed_points.ipynb` top-to-bottom; the figure is written to `figures/spurious_fixed_points.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** The compute is separated into a REGENERATE_CACHES-gated cell (compute_spurious_analysis) that writes data/spurious_fixed_points.npz, and a SAVE_FIGURES-gated plot cell that reads it; the compute cell seeds via ring.seed.set_global_seed(42), giving a deterministic run of about 2 minutes. The figure has two panel rows at five matched noise levels, a for the generative network and b for the data-derived network. The data-derived network converges from every tested initial condition, including per-neuron std 1000 where states start about 22 ring radii out, ending within 2e-4 of a ring radius of the manifold with no divergences observed; under unstructured initial conditions it also shows a bump-angle preference that weakens as the initialization noise grows, noted in the caption.

## Figure S12 — Finite-N attractor dynamics

- **Manuscript file:** `figures/finite_size_dynamics.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/spurious_fixed_points.ipynb`
- **Output:** `finite_size_dynamics.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/finite_size_dynamics.pdf"`
- **Reproduce:** run `notebooks/spurious_fixed_points.ipynb` top-to-bottom; the figure is written to `figures/finite_size_dynamics.pdf`.
- **Verification:** PDF-METADATA-ONLY
- **Notes:** Produced by one SAVE_FIGURES-gated code cell; there is no cached input, only in-notebook computation. The preceding cell seeds np.random.seed(42) and torch.manual_seed(42) (plus the CUDA variants) before any random draw, so the run is fully deterministic on both CPU and GPU. Regenerating reproduces the raster content exactly at 300 dpi; any byte differences are confined to PDF metadata.

## Figure S13 — Normalized covariance matrices and their eigenvectors

- **Manuscript file:** `figures/normalized_covariance_eigenvectors.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/data_and_generative_model.ipynb`
- **Output:** `normalized_covariance_eigenvectors.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/normalized_covariance_eigenvectors.png"`
- **Input caches:** `data/mouse_data/tc_data.npz`
- **Reproduce:** run `notebooks/data_and_generative_model.ipynb` top-to-bottom; the figure is written to `figures/normalized_covariance_eigenvectors.png`.
- **Verification:** BIT-EXACT
- **Notes:** Produced by a cell that sets np.random.seed(42) immediately before its one np.random.choice call, so the panel selection is deterministic. It reads tc_data.npz, which regenerates bit-exactly. Panel letters are lowercase, a and b.

## Figure S14 — Data and subsampled-data eigenvalue/singular-value spectra

- **Manuscript file:** `figures/data_and_subsample_spectra.png`
- **Kind:** composite
- **Producer notebook:** `notebooks/data_driven_reconstruction.ipynb`
- **Output:** `data_and_subsample_spectra.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/data_and_subsample_spectra.png", bbox_inches="tight", dpi=400)  # SAVE_FIGURES-gated`
- **Input caches:** `data/mouse_data/tc_data.npz`
- **Reproduce:** run `notebooks/data_driven_reconstruction.ipynb` top-to-bottom; the figure is written to `figures/data_and_subsample_spectra.png`.
- **Composite panels:**
    - a (data-derived spectrum, N=1533): mouse-data J/Phi computed in the same notebook; deterministic
    - b (convergence with sample size, N=100..1533): idealized subsample computation in the compose cell, local seed(42)
    - composed by one SAVE_FIGURES-gated code cell -> figures/data_and_subsample_spectra.png
- **Verification:** BIT-EXACT
- **Notes:** The whole figure is assembled by one SAVE_FIGURES-gated compose cell in data_driven_reconstruction.ipynb. Panel a (data) reuses the in-memory mouse-derived J and Phi computed earlier in the same notebook, with no random component. Panel b (subsample) computes idealized tuning curves and weights at N = 100, 500, 1000, 1533 with np.random.seed(42) set immediately before the N-loop, using ridge value lambda_sub = 1e-8; it is a seeded stochastic realization rather than an exact function of the data, though the same seed reproduces it deterministically. Panel letters are lowercase, a and b.

## Figure S15 — Eigenvalue spectrum of the grid-cell connectivity

- **Manuscript file:** `figures/grid_cell_eigenvalues.pdf`
- **Kind:** direct
- **Producer notebook:** `notebooks/grid_sims.ipynb`
- **Output:** `grid_cell_eigenvalues.pdf` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/grid_cell_eigenvalues.pdf"`
- **Reproduce:** run `notebooks/grid_sims.ipynb` top-to-bottom; the figure is written to `figures/grid_cell_eigenvalues.pdf`.
- **Verification:** NON-DETERMINISTIC
- **Notes:** The plotting cell shows the leading eigenvalues of the connectivity computed from generate_sample_data(N_samples=2500, N_period=1); no cache is read, and the sample data is seeded via the numpy RNG set at the top of the notebook. The spectrum is exactly 6-fold degenerate, and within each degenerate cluster the ordering returned by the eigenvalue solver is not guaranteed: it can change with the number of BLAS threads or between CPU and GPU execution, so a regenerated plot can show the same clear 6-fold degeneracy with individual points swapped within a cluster. This reordering is a non-deterministic artifact of the linear-algebra backend, not a change in the underlying spectrum.

## Figure S16 — Reservoir-RNN attractor dynamics (Darshan-Rivkind model)

- **Manuscript file:** `figures/reservoir_rnn_dynamics.png`
- **Kind:** direct
- **Producer notebook:** `notebooks/reservoir_rnn.ipynb`
- **Output:** `reservoir_rnn_dynamics.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/reservoir_rnn_dynamics.png", bbox_inches='tight', dpi=300)`
- **Reproduce:** run `notebooks/reservoir_rnn.ipynb` top-to-bottom; the figure is written to `figures/reservoir_rnn_dynamics.png`.
- **Verification:** BIT-EXACT
- **Notes:** Produced by a single cell that sets torch.manual_seed(42) and torch.cuda.manual_seed_all(42) before the ReservoirRNN is instantiated, draws the gain-row labels and the 'no convergence' box and shading in matplotlib, and saves figures/reservoir_rnn_dynamics.png directly. There is no cache; the reservoir weights, input projection, and initial state are drawn fresh from the seeded RNG on each run. torch's CPU and CUDA random-number streams differ even from an identical seed, so a CPU run produces a different draw than a GPU run, differing in about 10.6% of pixels, though the qualitative content (same gain sweep, same convergent and non-convergent regimes and boundary) is unchanged. The committed figure was generated on a GPU and is therefore not reproducible bit-for-bit from a CPU run; regenerate on a GPU to match it. Training five reservoirs from scratch makes this the most expensive figure in the repo to regenerate.

## Reproducibility caveats

Figures whose exact pixels are not bit-reproducible from the notebook alone. Each was reviewed and accepted during the equivalence pass; details in `EQUIVALENCE_LEDGER.md`.

- **Figure 6 (Circular geometry, disordered embedding, and spectra of the optimal weights).** J2 (the circulant-plus-noise matrix) is built by reseeding the RNG immediately before adding noise (np.random.seed(45)), and the whole figure is saved by the following code cell. The figure is fully synthetic: no cached array is read, and the tuning curves come from gen_tuning_curves_idealized. Reproducibility caveat: panel g's eigenvector/SVD panel can show a sign-flip in its rightmost subplot when regenerated on different hardware or BLAS builds, because singular vectors are only defined up to sign and the seed does not fix that. The spectra shown use ridge regularization lambda = 1e-8, computed as Gamma_x/(Gamma_phi + lambda); leading eigenvalues and the doublet/singlet structure are stable under this choice.
- **Figure S8 (PCA convergence of the data-driven ring attractor).** Produced by the PCA-convergence plotting cell (plot_pc_convergence_grid), gated by SAVE_FIGURES, which reads pc_convergence_grid.npz. Most of the cached arrays are exact recomputations, but the pcs_traj3_grid array is a stochastic realization, so trajectories drawn from it need not match bit-for-bit on a freshly regenerated cache even though the rest of the figure will. The sweep is over the ridge-regularization strength lambda, with the operating value lambda = 1e-8. Regenerating under a different matplotlib/Python patch version can reassign the categorical color cycle, shifting trajectory hue and draw order with no change to trajectory geometry, so small color-cycle-driven pixel differences across environments are expected.
- **Figure S15 (Eigenvalue spectrum of the grid-cell connectivity).** The plotting cell shows the leading eigenvalues of the connectivity computed from generate_sample_data(N_samples=2500, N_period=1); no cache is read, and the sample data is seeded via the numpy RNG set at the top of the notebook. The spectrum is exactly 6-fold degenerate, and within each degenerate cluster the ordering returned by the eigenvalue solver is not guaranteed: it can change with the number of BLAS threads or between CPU and GPU execution, so a regenerated plot can show the same clear 6-fold degeneracy with individual points swapped within a cluster. This reordering is a non-deterministic artifact of the linear-algebra backend, not a change in the underlying spectrum.
- **Figure S16 (Reservoir-RNN attractor dynamics (Darshan-Rivkind model)).** Produced by a single cell that sets torch.manual_seed(42) and torch.cuda.manual_seed_all(42) before the ReservoirRNN is instantiated, draws the gain-row labels and the 'no convergence' box and shading in matplotlib, and saves figures/reservoir_rnn_dynamics.png directly. There is no cache; the reservoir weights, input projection, and initial state are drawn fresh from the seeded RNG on each run. torch's CPU and CUDA random-number streams differ even from an identical seed, so a CPU run produces a different draw than a GPU run, differing in about 10.6% of pixels, though the qualitative content (same gain sweep, same convergent and non-convergent regimes and boundary) is unchanged. The committed figure was generated on a GPU and is therefore not reproducible bit-for-bit from a CPU run; regenerate on a GPU to match it. Training five reservoirs from scratch makes this the most expensive figure in the repo to regenerate.

## Additional figures

Figures produced by the analysis notebooks that are not numbered figures in the paper.

### R1 — Weight-matrix spectrum vs. theory at N=1533

- **Manuscript file:** `figures/weight_spectrum_vs_theory.png`
- **Kind:** response
- **Producer notebook:** `notebooks/weight_matrix_analyses.ipynb`
- **Output:** `weight_spectrum_vs_theory.png` (written to `figures/`)
- **Producer cell:** the cell containing `plt.savefig("../figures/weight_spectrum_vs_theory.png"`
- **Reproduce:** run `notebooks/weight_matrix_analyses.ipynb` top-to-bottom; the figure is written to `figures/weight_spectrum_vs_theory.png`.
- **Verification:** BIT-EXACT
- **Notes:** An additional figure overlaying the eigenvalues of the weight matrix J with the covariance-based prediction alpha * lambda^x_n / lambda^phi_n, for the generative-process network at N = 1533, showing that the circulant approximation holds at this network size. It is produced by the final cell of weight_matrix_analyses.ipynb, seeded with np.random.seed(42) for determinism; the committed PNG is this seeded output. The theoretical prediction curve is unregularized and does not change on regeneration; only the measured eigenvalues of J depend on the seed.

